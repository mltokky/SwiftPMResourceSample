import XCTest
@testable import SwiftPMResourceSample

final class SwiftPMResourceSampleTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(SwiftPMResourceSample().text, "Hello, World!")
    }
}
